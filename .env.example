# Local developer environment variables.
#
# Copy to `.env` and fill in your own values. `.env` is gitignored.
#
# Linear personal API key (Settings → API → Personal API keys):
# LINEAR_API_KEY="lin_api_..."
LINEAR_API_KEY=""

# ChatGPT/Codex bearer token (used by `tools/codex_autopr.py` to auto-create PRs from completed Codex tasks).
# Note: this is a *session* token and may expire; refresh it if the script starts failing with 401/403.
CODEX_TOKEN=""

# GitHub personal access token (classic) or fine-grained token with access to this repo.
# Used by `tools/github_automerge.py` to read PR/check status + update branches + merge PRs.
GITHUB_TOKEN=""
